{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
{# {{ form_start(form, {'attr': {'class': 'form-horizontal p-4 bg-light rounded shadow'}}) }}
    <div class="form-group">
        {{ form_label(form.nom, null, {'label_attr': {'class': 'control-label text-primary font-weight-bold'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.nom, {'attr': {'class': 'form-control border border-primary'}}) }}
        </div>
    </div>
    
    <div class="form-group mt-3">
        {{ form_label(form.description, null, {'label_attr': {'class': 'control-label text-success font-weight-bold'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.description, {'attr': {'class': 'form-control border border-success'}}) }}
        </div>
    </div>

    <div class="form-group text-center mt-4">
        <button class="btn btn-info btn-lg">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }} #}
{% block body %}
<div class="container mt-5">
    

    {{ form_start(form, {'attr': {'class': 'row g-3'}}) }}

        <div class="col-md-6">
            {{ form_row(form.nom, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="col-md-6">
            {{ form_row(form.description, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="col-12 text-center mt-4">
            <button type="submit" class="btn btn-primary">Envoyer</button>
        </div>

    {{ form_end(form) }}     
</div>
{% endblock %}
