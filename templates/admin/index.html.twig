{% extends 'base.html.twig' %}

{% block title %}Espace Administrateur{% endblock %}

{% block body %}
{# <style>

</style>

<div class="container admin-container">
    <div class="row admin-header">
        <div class="col-md-12">
            <h1 class="display-4">Bonjour, José</h1>
            <h2>Bienvenue dans votre espace administrateur</h2>
            <h3>Que souhaitez-vous effectuer?</h3>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="card admin-card">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_services_index') }}">
                    Modifier les services
                </a>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card admin-card">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_register') }}">
                    Création de compte
                </a>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card admin-card">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_habitat_index') }}">
                    Modifier les habitats
                </a>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="card admin-card">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_animal_index') }}">
                    Modifier les animaux
                </a>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card admin-card">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_opening_hours_index') }}">
                    Modifier les horaires
                </a>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card admin-card">
                <a class="btn btn-danger admin-btn text-white" href="{{ url('app_logout') }}">
                    Déconnexion
                </a>
            </div>
        </div>
    </div>
</div>
 <div class="row table-container">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Consultations des Animaux</h4>
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>Nom de l'animal</th>
                                    <th>Nombre de consultations</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for animal in animals %}
                                    <tr>
                                        <td>{{ animal.name }}</td>
                                        <td id="consultation-count-{{ animal.id }}">
                                            {% for animalCount in animalsCount %}
                                                {% if animalCount.animalId == animal.id %}
                                                    {{ animalCount.consultationCount }}
                                                {% endif %}
                                            {% endfor %}
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="row table-container">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Comptes Rendus Vétérinaires</h4>
                    <form method="get" class="filters">
                        <select name="animalId">
                            <option value="">-- Filtrer par animal --</option>
                            {% for animal in animals %}
                                <option value="{{ animal.id }}" {% if selectedAnimalId == animal.id %}selected{% endif %}>
                                    {{ animal.name }}
                                </option>
                            {% endfor %}
                        </select>
                        <input type="date" name="date" value="{{ selectedDate|date('Y-m-d') }}" />
                        <button type="submit" class="btn btn-primary">Filtrer</button>
                    </form>
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>Animal</th>
                                    <th>Date</th><th>État</th>
                                    <th>Détail</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for rapport in rapportsVeterinaires %}
                                    <tr>
                                        <td>{{ rapport.getAnimal().getName() }}</td>
                                        <td>{{ rapport.getDate()|date('d/m/Y') }}</td>
                                        <td>{{ rapport.getState() }}</td>
                                        <td>{{ rapport.getDetail() }}</td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="4">Aucun rapport trouvé pour les critères sélectionnés.</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

{% endblock %} #}
<div class="container admin-container mt-4 mb-4">
    <div class="row admin-header text-center mb-4">
        <div class="col-12">
            <h1 class="display-4">Bonjour, José</h1>
            <h2>Bienvenue dans votre espace administrateur</h2>
            <h3>Que souhaitez-vous effectuer?</h3>
        </div>
    </div>

    <div class="row g-4"> <!-- Ajout de g-4 pour espacement entre colonnes -->
        <div class="col-lg-4 col-md-6 col-sm-12">
            <div class="card admin-card text-center">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_services_index') }}">
                    Modifier les services
                </a>
            </div>
        </div>

        <div class="col-lg-4 col-md-6 col-sm-12">
            <div class="card admin-card text-center">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_register') }}">
                    Création de compte
                </a>
            </div>
        </div>

        <div class="col-lg-4 col-md-6 col-sm-12">
            <div class="card admin-card text-center">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_habitat_index') }}">
                    Modifier les habitats
                </a>
            </div>
        </div>

        <div class="col-lg-4 col-md-6 col-sm-12">
            <div class="card admin-card text-center">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_animal_index') }}">
                    Modifier les animaux
                </a>
            </div>
        </div>

        <div class="col-lg-4 col-md-6 col-sm-12">
            <div class="card admin-card text-center">
                <a class="btn btn-primary admin-btn text-white" href="{{ url('app_opening_hours_index') }}">
                    Modifier les horaires
                </a>
            </div>
        </div>

        <div class="col-lg-4 col-md-6 col-sm-12">
            <div class="card admin-card text-center">
                <a class="btn btn-danger admin-btn text-white" href="{{ url('app_logout') }}">
                    Déconnexion
                </a>
            </div>
        </div>
    </div>
</div>
<div class="row table-container mt-4">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title text-center">Consultations des Animaux</h4>
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Nom de l'animal</th>
                                <th>Nombre de consultations</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for animal in animals %}
                                <tr>
                                    <td>{{ animal.name }}</td>
                                    <td id="consultation-count-{{ animal.id }}">
                                        {% for animalCount in animalsCount %}
                                            {% if animalCount.animalId == animal.id %}
                                                {{ animalCount.consultationCount }}
                                            {% endif %}
                                        {% endfor %}
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="row table-container mt-4">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title text-center">Comptes Rendus Vétérinaires</h4>
                <form method="get" class="filters mb-3 d-flex flex-wrap justify-content-between">
                    <select class="form-select mb-2" name="animalId">
                        <option value="">-- Filtrer par animal --</option>
                        {% for animal in animals %}
                            <option value="{{ animal.id }}" {% if selectedAnimalId == animal.id %}selected{% endif %}>
                                {{ animal.name }}
                            </option>
                        {% endfor %}
                    </select>
                    <input type="date" class="form-control mb-2" name="date" value="{{ selectedDate|date('Y-m-d') }}" />
                    <button type="submit" class="btn btn-primary mb-2">Filtrer</button>
                </form>
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Animal</th>
                                <th>Date</th>
                                <th>État</th>
                                <th>Détail</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for rapport in rapportsVeterinaires %}
                                <tr>
                                    <td>{{ rapport.getAnimal().getName() }}</td>
                                    <td>{{ rapport.getDate()|date('d/m/Y') }}</td>
                                    <td>{{ rapport.getState() }}</td>
                                    <td>{{ rapport.getDetail() }}</td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="4" class="text-center">Aucun rapport trouvé pour les critères sélectionnés.</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}

