{% extends 'base.html.twig' %}

{% block title %}Edit Services{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Modifier les services</h1>

        <div class="mb-3">
            {{ include('services/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>

        <a href="{{ path('app_services_index') }}" class="btn btn-secondary">
            Retourner à la liste
        </a>


        {# 
        {{ include('opening_hours/_delete_form.html.twig', { 'attr': {'class': 'd-inline'} }) }} 
        <button class="btn btn-danger">Supprimer</button>
        #}
    </div>
{% endblock %}