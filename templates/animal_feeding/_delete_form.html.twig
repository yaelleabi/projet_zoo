<form method="post" action="{{ path('app_animal_feeding_delete', {'id': animal_feeding.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal_feeding.id) }}">
    <button class="btn">Delete</button>
</form>
